// ════════════════════════════════════════════════════════
//  PDA 2 — Wifi_Class_sim.cpp
//  На PC WiFi физически не существует — состояние не имитируем,
//  просто не делаем ничего. PDA.Prefs.setWifi() (вызывается из
//  QuickPanel_Class, не отсюда) всё равно сохраняет выбор
//  пользователя, как и на железе.
//
//  Компилируется, только если PDA2_SIM определён —
//  на ESP32 своя реализация (см. Wifi_Class_esp32.cpp).
// ════════════════════════════════════════════════════════

#include "Wifi_Class.h"

#ifdef PDA2_SIM

void Wifi_Class::setEnabled(bool on) {
    (void)on;
}

#endif // PDA2_SIM