#pragma once

// ════════════════════════════════════════════════════════
//  PDA 2 — Wifi_Class
//  Минимальная Subsystem-обёртка над WiFi (закрывает нарушение
//  Правила 4 — раньше QuickPanel_Class дёргал WiFi.mode() напрямую).
//
//  Только то, что реально используется — включение/выключение
//  радио из QuickPanel. Ничего для сканирования/подключения —
//  этого в проекте пока нигде нет.
// ════════════════════════════════════════════════════════

class Wifi_Class {
public:
    void setEnabled(bool on);
};