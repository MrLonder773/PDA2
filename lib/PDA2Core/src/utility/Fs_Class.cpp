// ════════════════════════════════════════════════════════
//  PDA 2 — Fs_Class.cpp
//  Обёртка Fs_Class: делегирует в internal/sd, которые уже
//  расщеплены на _esp32/_sim. Сама обёртка platform-независима,
//  поэтому один файл на обе платформы (см. правило в доке:
//  "классы без реальной переносимой логики → полный сплит,
//  классы с ней → один .cpp с platform-хуками" — здесь логики
//  вообще нет, просто делегирование, сплит не нужен).
// ════════════════════════════════════════════════════════

#include "Fs_Class.h"

void Fs_Class::begin() {
    internal.begin();
    sd.begin();
}

bool Fs_Class::sdAvailable() {
    return sd.ok();
}